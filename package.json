{
  "name": "tide-database",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "sources/*"
  ],
  "scripts": {
    "build": "npm run build -w @neaps/tide-database",
    "test": "tsc -p tsconfig.node.json && npm run test -w @neaps/tide-database -w @neaps/datums -w @neaps/harmonic-analysis -w @neaps/stations",
    "lint": "prettier --check .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "prettier": "^3.7.4",
    "typescript": "^7.0.2"
  }
}
