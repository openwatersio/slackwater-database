{
  "name": "tide-database",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @neaps/tide-database",
    "test": "tsc -p tsconfig.node.json && npm run test -w @neaps/tide-database -w @tide-database/datums -w @tide-database/harmonic-analysis -w @tide-database/stations",
    "lint": "prettier --check .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/make-fetch-happen": "^10.0.4",
    "@types/node": "^26.0.0",
    "make-fetch-happen": "^16.0.0",
    "moment": "^2.30.1",
    "prettier": "^3.7.4",
    "typescript": "^7.0.2"
  }
}
