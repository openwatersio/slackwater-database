{
  "name": "swift",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "./generate",
    "pretest": "npm run --prefix ../database generate && node --experimental-transform-types scripts/generate-fixture.ts",
    "test": "swift test"
  }
}
