{
  "name": "tcd",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "./build",
    "pretest": "npm run build",
    "test": "vitest"
  },
  "devDependencies": {
    "@slackwater/database": "*",
    "@slackwater/engine": "1.0.0-beta.0",
    "vitest": "^5.0.1"
  }
}
