#!/bin/bash

set -e

# Remove any existing files to ensure a clean build
rm -rf dist

# Build the harmonics and offsets files
node build.ts

# Build the .tcd file
docker compose run --rm build-tcd dist/harmonics.tcd dist/harmonics.txt dist/offsets.xml
